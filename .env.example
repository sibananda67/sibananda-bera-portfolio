# Portfolio Environment Configuration
# NOTE: All variables are completely OPTIONAL.
# The portfolio runs in full functional capacity without requiring any API keys or secrets.
# - GEMINI_API_KEY: Optional server-side key for live AI. If not supplied, SB AI runs in Demo Mode.
# - VITE_GA_MEASUREMENT_ID: Optional Google Analytics 4 ID. If not supplied, analytics is disabled.
